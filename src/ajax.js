/**
 * @fileoverview
 * @author FE개발팀
 */

(function(ne) {
    'use strict';
    if (!ne) {
        ne = window.ne = {};
    }

    /**
     * @todo 이제인
     * pug.net.ajax() 작업 부탁드립니다.
     */

    //ne.ajax = ajax;

})(window.ne);